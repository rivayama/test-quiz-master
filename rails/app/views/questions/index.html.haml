.container
  %h1 Choose a question!

  .well
    %ul
      - @questions.each do |question|
        %li
          = link_to strip_markdown(question.question).lines.first, question
          %span.small
            (#{link_to "edit", edit_question_path(question)})

  = link_to "Add a new question", new_question_path
