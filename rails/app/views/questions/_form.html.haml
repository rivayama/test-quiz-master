= form_for @question do |f|

  - if @question.errors.present?
    .alert.alert-danger
      - @question.errors.full_messages.each do |message|
        .message= message

  .form-group
    = f.label :question
    = f.text_area :question, class: 'form-control', rows: 8, data: {provide: 'markdown', iconlibrary: 'fa', hidden_buttons: 'cmdHeading'}
    %p.help-block The first line will be shown on the index page.

  .form-group
    = f.label :answer
    = f.text_field :answer, class: 'form-control'

  = f.submit nil, class: 'btn btn-primary'
